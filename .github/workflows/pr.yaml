# draLLaM Pull Request pipeline
# Runs when a commit on a feature branch is pushed up to GitHub.
#
# (c) 2024 Alberto Morón Hernández
name: PR

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches-ignore:
      - main

env:
  GH_TOKEN: ${{ secrets.GH_TOKEN }}

jobs:
  pre_commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/pre-commit

  open-release-pr:
    # run only for pushes to a 'release-X-Y-Z' branch
    # the process to create these branches is automated via `make release v=X.Y.Z`
    if: ${{ github.event_name == 'push' && startsWith(github.ref_name, 'release-') }}
    needs:
      - pre_commit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: gh pr create --base main --title "${{ github.ref_name }}" --body ""
        shell: bash
    # TODO: explore reinstating `gh pr merge --auto --rebase`
